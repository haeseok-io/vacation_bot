const puppeteer = require('puppeteer');
const cheerio = require('cheerio');

function userData (username){
    const result = {id: '', name: ''};
    const url = "https://sa.nexon.com/ranking/total/ranklist.aspx?strSearch=";

    return 'abcp';
}

module.exports = {
    userData
};